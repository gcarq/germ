[package]
name = "pkgrove"
version.workspace = true
edition.workspace = true

[dependencies]
anyhow.workspace = true
log.workspace = true
clap = { version = "4.6.0", default-features = false, features = ["color", "error-context", "help", "std", "usage", "derive"] }
colored = { version = "3.1.1", default-features = false }
fern = { version = "0.7.1", default-features = false, features = ["colored"] }
pkgrove-core = { path = "../pkgrove-core" }

[lints]
workspace = true